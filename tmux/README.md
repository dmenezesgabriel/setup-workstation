# tmux config

Copy `.tmux.conf` to your home directory:

```bash
cp tmux/.tmux.conf ~/.tmux.conf
```

Then reload the config inside tmux:

```
prefix + r
```

Or from the terminal:

```bash
tmux source-file ~/.tmux.conf
```
